# ERC20 Token

```shell
// Deploy contract to goerli testnet
npx hardhat run scripts/deploy.js --network goerli

// Do an airdrop to another address
npx hardhat run scripts/airdrop.js --network goerli
```
